import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    // Tests will be added in Phase 1 once the first feature is complete.
    expect(true, isTrue);
  });
}
